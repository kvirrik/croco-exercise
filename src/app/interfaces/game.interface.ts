export interface Game {
  tags: string[];
  order: number;
  url: string;
  image: string;
  providerName: string;
  provider: string;
  name: string;
  game_id: string;
  gameId: string;
  image2: string;
}
